// ⚠️ LEGACY — plus utilisé nulle part dans l'app depuis la convergence vers FSRS.
// Conservé pour référence historique uniquement. Ne PAS ré-importer.
// Source de vérité actuelle : src/lib/fsrs.js (scheduling) + src/lib/reviewStats.js (stats).
// La migration one-shot des révisions SM-2 orphelines vit dans src/lib/db/migration.js
// (migrateOrphanSRSData) et lit directement localStorage — n'a plus besoin de ce fichier.

